# direnv .envrc - reads .phpversion and prepends the matching PHP to PATH
if [ -f .phpversion ]; then
  php_version=$(cat .phpversion | tr -d '[:space:]' | tr -d '.')
  php_path="/usr/lib/php/php${php_version}"
  if [ -d "$php_path" ]; then
    PATH_add "$php_path"
  fi
fi